#include<iostream>
#include<ctime>
using namespace std;

int main()
{
	srand(time(0));
	int n;
	
	//生成 a<=n<=b 的随机数  对（b-a+1)取模 再减去或加上xx得到想要的范围

	//如-15<=n<=19
	n = rand() % 35 - 15;
	cout << n;


	system("pause");
	return 0;
}
